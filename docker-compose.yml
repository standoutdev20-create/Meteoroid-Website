services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: meteoroid-website
    container_name: meteoroid-website
    ports:
      - "${HOST_PORT:-3002}:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
